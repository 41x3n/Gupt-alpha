# Gupt-alpha

## Steps to run the application
- `cd` into client 
  - run `npm install`
  - run `npm run dev`
- `cd` into backend folder
  - Set up .env file with db URL 
  - run `npm install`
  - run `npm start`
